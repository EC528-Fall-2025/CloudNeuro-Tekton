apiVersion: batch/v1
kind: Job
metadata:
  name: emerald-extraction
  namespace: chris-students-c9344e
spec:
  template:
    spec:
      containers:
      - name: emerald
        image: ghcr.io/fnndsc/pl-emerald:latest
        args: 
          - "emerald"
          - "--pattern"
          - "DICOM_SAG_MPRAGE_220_FOV_20130308074224_5.nii.gz"
          - "--mask-suffix"
          - "_mask.nii"
          - "/data/NIFTI"
          - "/data/EMERALD"
        volumeMounts:
        - name: dicom-storage
          mountPath: /data
      restartPolicy: Never
      volumes:
      - name: dicom-storage
        persistentVolumeClaim:
          claimName: dicom-pvc
  backoffLimit: 2
